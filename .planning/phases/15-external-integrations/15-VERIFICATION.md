# Phase 15 Verification

## API Keys Checks
- [x] Create a new API Key and ensure the unhashed secret is only shown once.
- [x] Verify API Key listing displays correct metadata (name, partial key, creation date).
- [x] Revoke an API Key and confirm it is immediately invalidated.

## Webhooks Checks
- [x] Add a new webhook endpoint and select specific events to subscribe to.
- [x] Verify the webhook payload structure matching the subscribed events.
- [x] Validate signature signing and endpoint verification (QStash).
